package vce.nhs.pomodolock

/*
import androidx.test.core.app.ActivityScenario
import androidx.test.platform.app.InstrumentationRegistry
import org.junit.Before
import org.junit.Test
import org.junit.runner.RunWith
import androidx.test.espresso.Espresso.onView
import org.robolectric.RobolectricTestRunner

@RunWith(RobolectricTestRunner::class)
class TodoFragmentUnitTest {


    @Before
    fun setup() {
        // Create an in-memory database for testing
        val context = InstrumentationRegistry.getInstrumentation().targetContext

        // Launch the activity containing the TodoFragment
        ActivityScenario.launch(MainActivity::class.java)
    }


    @Test
    fun testAddTaskWithDatabaseChange() {
        // Perform a button click action to open AddTaskFragment
        onView(withId(R.id.addTaskButton)).perform(click())

        // Enter a task name in the AddTaskFragment's input field
        onView(withId(R.id.editTextTask)).perform(typeText("Testing#42"))

        // Click the "Add" button in the AddTaskFragment
        onView(withId(R.id.buttonAdd)).perform(click())
    }
}
*/
